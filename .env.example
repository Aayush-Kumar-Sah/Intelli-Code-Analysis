# Environment Configuration for Intelli-Code-Analysis

# NODE_ENV - Development environment setting
# Set to "development" to enable debug logging to stderr
# Set to "production" or leave unset for production mode (no debug logs)
# Default: production (if not set)
NODE_ENV=production

# Example for development:
# NODE_ENV=development
