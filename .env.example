# Environment Configuration for Intelli-Code-Analysis

# NODE_ENV - Development environment setting (OPTIONAL)
# Uncomment and set to "development" to enable debug logging to stderr
# When not set or set to "production", runs in production mode with no debug logs
# Default: production (if not set)

# For development (uncomment to enable):
# NODE_ENV=development

# For production (default behavior, no need to set):
# NODE_ENV=production
